# Databricks Connect for local development (compatible with runtime 14.3)
databricks-connect>=14.3.0

# PySpark (managed by Databricks Connect, but specified for compatibility)
pyspark>=3.5.0

# Delta Lake for Delta table operations
delta-spark>=3.0.0

# Redis client with SSL support
redis>=4.5.0

# Core Python dependencies
typing-extensions>=4.5.0

# Databricks SDK (optional, for advanced Databricks operations)
databricks-sdk>=0.18.0

# Development and testing dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Documentation (optional)
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0

# Additional dependencies for Databricks environment
pyarrow>=10.0.0
pandas>=2.0.0